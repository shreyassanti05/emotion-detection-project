# emotion-detection-project
This project uses the Watson NLP API to analyze the dominant emotion in a given text. It is built using Python, packaged into the `EmotionDetection` module, and deployed via a Flask web application.
